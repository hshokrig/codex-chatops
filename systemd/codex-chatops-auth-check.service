[Unit]
Description=Codex auth health check

[Service]
Type=oneshot
# Template only. Adjust the checkout path for your own machine before enabling.
WorkingDirectory=%h/Projects/codex-chatops
ExecStart=/bin/bash -lc 'cd %h/Projects/codex-chatops && codex login status'
